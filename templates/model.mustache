# coding: utf-8

from __future__ import absolute_import
from datetime import date, datetime  # noqa: F401

from typing import List, Dict  # noqa: F401

from {{modelPackage}}.base_model_ import Model
{{#imports}}{{import}}  # noqa: F401,E501
{{/imports}}
from {{packageName}} import util


{{#models}}
{{#model}}
class {{classname}}(Model):
    """NOTE: This class is auto generated by the swagger code generator program.

    Do not edit the class manually.
    """{{#allowableValues}}

    """
    allowed enum values
    """
{{#enumVars}}
    {{name}} = {{{value}}}{{^-last}}
{{/-last}}
{{/enumVars}}{{/allowableValues}}

    def __init__(self{{#vars}}, {{name}}{{^supportPython2}}: {{datatype}}{{/supportPython2}}={{#defaultValue}}{{{defaultValue}}}{{/defaultValue}}{{^defaultValue}}None{{/defaultValue}}{{/vars}}):  # noqa: E501
        """{{classname}} - a model defined in Swagger

        {{#vars}}
        :param {{name}}: The {{name}} of this {{classname}}.  # noqa: E501
        :type {{name}}: {{datatype}}
        {{/vars}}
        """
        self.swagger_types = {
{{#vars}}
            '{{name}}': {{{datatype}}}{{#hasMore}},{{/hasMore}}
{{/vars}}
        }

        self.attribute_map = {
{{#vars}}
            '{{name}}': '{{baseName}}'{{#hasMore}},{{/hasMore}}
{{/vars}}
        }
{{#vars}}{{#-first}}
{{/-first}}
        self._{{name}} = {{name}}
{{/vars}}

    @classmethod
    def from_dict(cls, dikt){{^supportPython2}} -> '{{classname}}'{{/supportPython2}}:
        """Returns the dict as a model

        :param dikt: A dict.
        :type: dict
        :return: The {{name}} of this {{classname}}.  # noqa: E501
        :rtype: {{classname}}
        """
        return util.deserialize_model(dikt, cls)

    def validate(self):
        errors = []
        {{#vars}}
{{#isEnum}}
        allowed_values = [{{#allowableValues}}{{#values}}"{{{this}}}"{{^-last}}, {{/-last}}{{/values}}{{/allowableValues}}]  # noqa: E501
{{#isContainer}}
{{#isListContainer}}
        if not set(self._{{{name}}}).issubset(set(allowed_values)):
            errors.append({
                "code": "isListContainer",
                "field": "{{name}}",
                "message": "Invalid values for `{{{name}}}` [{0}], must be a subset of [{1}]".format(  # noqa: E501
                    ", ".join(map(str, set({{{name}}}) - set(allowed_values))),  # noqa: E501
                    ", ".join(map(str, allowed_values)))
            })
{{/isListContainer}}
{{#isMapContainer}}
        if not set(self._{{{name}}}.keys()).issubset(set(allowed_values)):
            errors.append({
                "code": "isMapContainer",
                "field": "{{name}}",
                "message": "Invalid keys in `{{{name}}}` [{0}], must be a subset of [{1}]".format(  # noqa: E501
                    ", ".join(map(str, set(self._{{{name}}}.keys()) - set(allowed_values))),  # noqa: E501
                    ", ".join(map(str, allowed_values)))
            })
{{/isMapContainer}}
{{/isContainer}}
{{^isContainer}}
        if self._{{{name}}} not in allowed_values:
            errors.append({
                "code": "isContainer",
                "field": "{{name}}",
                "message": "Invalid value for `{{{name}}}` ({0}), must be one of {1}".format(
                    self._{{{name}}}, allowed_values)  # noqa: E501
            })
{{/isContainer}}
{{/isEnum}}
{{^isEnum}}
{{#required}}
        if self._{{name}} is None:
            errors.append({
                "code": "required",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, must not be `None`"  # noqa: E501
            })
{{/required}}
{{#isInteger}}
        if self._{{name}} is not None and self.is_not_int(self._{{name}}):
            errors.append({
                "code": "type",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, type must be `integer`"  # noqa: E501
            })
{{/isInteger}}
{{#isLong}}
        if self._{{name}} is not None and self.is_not_int(self._{{name}}):
            errors.append({
                "code": "type",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, type must be `integer`"  # noqa: E501
            })
{{/isLong}}
{{#isFloat}}
        if self._{{name}} is not None and self.is_not_float(self._{{name}}):
            errors.append({
                "code": "type",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, type must be `float`"  # noqa: E501
            })
{{/isFloat}}
{{#isDouble}}
        if self._{{name}} is not None and self.is_not_float(self._{{name}}):
            errors.append({
                "code": "type",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, type must be `float`"  # noqa: E501
            })
{{/isDouble}}
{{#isBoolean}}
        if self._{{name}} is not None and self.is_not_bool(self._{{name}}):
            errors.append({
                "code": "type",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, type must be `boolean`"  # noqa: E501
            })
{{/isBoolean}}
{{#isDateTime}}
        if self._{{name}} is not None and self.is_not_datetime(self._{{name}}):
            errors.append({
                "code": "type",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, type must be `datetime`"  # noqa: E501
            })
{{/isDateTime}}
{{#hasValidation}}
{{#maxLength}}
        if self._{{name}} is not None and len(self._{{name}}) > {{maxLength}}:
            errors.append({
                "code": "maxLength",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, length must be less than or equal to `{{maxLength}}`"  # noqa: E501
            })
{{/maxLength}}
{{#minLength}}
        if self._{{name}} is not None and len(self._{{name}}) < {{minLength}}:
            errors.append({
                "code": "minLength",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, length must be greater than or equal to `{{minLength}}`"  # noqa: E501
            })
{{/minLength}}
{{#maximum}}
        if self._{{name}} is not None and self._{{name}} >{{#exclusiveMaximum}}={{/exclusiveMaximum}} {{maximum}}:  # noqa: E501
            errors.append({
                "code": "maximum",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, must be a value less than {{^exclusiveMaximum}}or equal to {{/exclusiveMaximum}}`{{maximum}}`"  # noqa: E501
            })
{{/maximum}}
{{#minimum}}
        if self._{{name}} is not None and self._{{name}} <{{#exclusiveMinimum}}={{/exclusiveMinimum}} {{minimum}}:  # noqa: E501
            errors.append({
                "code": "minimum",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, must be a value greater than {{^exclusiveMinimum}}or equal to {{/exclusiveMinimum}}`{{minimum}}`"  # noqa: E501
            })
{{/minimum}}
{{#pattern}}
        if self._{{name}} is not None and not re.search('{{{vendorExtensions.x-regex}}}', self._{{name}}{{#vendorExtensions.x-modifiers}}{{#-first}}, flags={{/-first}}re.{{.}}{{^-last}} | {{/-last}}{{/vendorExtensions.x-modifiers}}):  # noqa: E501
            errors.append({
                "code": "pattern",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, must be a follow pattern or equal to `{{{pattern}}}`"  # noqa: E501
            })
{{/pattern}}
{{#maxItems}}
        if self._{{name}} is not None and len(self._{{name}}) > {{maxItems}}:
            errors.append({
                "code": "maxItems",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, number of items must be less than or equal to `{{maxItems}}`"  # noqa: E501
            })
{{/maxItems}}
{{#minItems}}
        if self._{{name}} is not None and len(self._{{name}}) < {{minItems}}:
            errors.append({
                "code": "minItems",
                "field": "{{name}}",
                "message": "Invalid value for `{{name}}`, number of items must be greater than or equal to `{{minItems}}`"  # noqa: E501
            })
{{/minItems}}
{{/hasValidation}}
{{/isEnum}}
        {{/vars}}
        return errors{{#vars}}{{#-first}}

{{/-first}}
    @property
    def {{name}}(self){{^supportPython2}} -> {{datatype}}{{/supportPython2}}:
        """Gets the {{name}} of this {{classname}}.

        {{#description}}
        {{{description}}}  # noqa: E501
        {{/description}}

        :return: The {{name}} of this {{classname}}.
        :rtype: {{datatype}}
        """
{{#isInteger}}
        if self.is_not_int(self._{{name}}):
            return None
{{/isInteger}}
{{#isLong}}
        if self.is_not_int(self._{{name}}):
            return None
{{/isLong}}
{{#isFloat}}
        if self.is_not_float(self._{{name}}):
            return None
{{/isFloat}}
{{#isDouble}}
        if self.is_not_float(self._{{name}}):
            return None
{{/isDouble}}
{{#isBoolean}}
        if self.is_not_bool(self._{{name}}):
            return None
{{/isBoolean}}
{{#isDateTime}}
        if self.is_not_datetime(self._{{name}}):
            return None
{{/isDateTime}}
        return self._{{name}}

    @{{name}}.setter
    def {{name}}(self, {{name}}{{^supportPython2}}: {{datatype}}{{/supportPython2}}):
        """Sets the {{name}} of this {{classname}}.

        {{#description}}
        {{{description}}}  # noqa: E501
        {{/description}}

        :param {{name}}: The {{name}} of this {{classname}}.
        :type {{name}}: {{datatype}}
        """
        self._{{name}} = {{name}}{{^-last}}

{{/-last}}
{{/vars}}

{{/model}}
{{/models}}
